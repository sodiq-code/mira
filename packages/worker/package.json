{
  "name": "@mira/worker",
  "version": "0.1.0",
  "private": true,
  "description": "Off-chain worker: Attestcoin proof generation/verification, agent decisioning, and Creditcoin contract interaction.",
  "main": "src/index.ts",
  "scripts": {
    "validate": "bun run scripts/validate-attestcoin.ts",
    "validate-llm": "bun run scripts/validate-llm.ts",
    "full-flow": "bun run scripts/full-flow.ts",
    "repayment-default": "bun run scripts/repayment-default.ts",
    "integrate": "bun run scripts/integrate.ts",
    "dev": "bun run src/index.ts",
    "build": "tsc"
  },
  "dependencies": {
    "@gluwa/usc-sdk": "^0.18.0",
    "@mira/shared": "*",
    "ethers": "^6.15.0",
    "z-ai-web-dev-sdk": "^0.0.18"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.2"
  }
}
