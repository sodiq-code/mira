{
  "name": "@mira/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Creditcoin smart contracts: policy bounds, loan lifecycle, and reputation ledgers.",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.ts --network creditcoin_testnet",
    "test": "hardhat test",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.9.2"
  }
}
